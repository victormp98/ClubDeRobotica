[providers]
python = {}
node = {}

[phases.setup]
nixPkgs = ["python311", "nodejs"]

[phases.install]
cmds = ["pip install -r requirements.txt", "npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "python -m flask db upgrade && gunicorn -w 4 -b 0.0.0.0:${PORT:-5000} run:app"
